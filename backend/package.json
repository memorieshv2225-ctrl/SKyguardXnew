{
  "name": "helmet-crash-backend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "build:app": "npm run build --prefix ../frontend",
    "start:app": "npm run build:app && node scripts/start-app.mjs",
    "ngrok": "node scripts/run-ngrok.mjs"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.4.2",
    "express": "^4.21.2"
  },
  "devDependencies": {
    "ngrok": "^5.0.0-beta.2"
  }
}
